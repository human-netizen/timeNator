name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore TimeNator.slnx

      - name: Build
        run: dotnet build TimeNator.slnx --no-restore -c Release

      - name: Test
        run: dotnet test TimeNator.slnx --no-build -c Release
